---
- name: common tasks
  hosts: "{{ playbook_hosts | default('localhost') }}"
  become: true
  vars:
    common_custom_sshd_config: false

  tasks:
    - ansible.builtin.import_role:
        name: common
